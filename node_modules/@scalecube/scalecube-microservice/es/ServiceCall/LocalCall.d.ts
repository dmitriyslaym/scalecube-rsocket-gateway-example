import { Observable } from 'rxjs';
import { LocalCallOptions } from '../helpers/types';
export declare const localCall: ({ localService, asyncModel, message, microserviceContext }: LocalCallOptions) => Observable<unknown> | Promise<unknown>;
