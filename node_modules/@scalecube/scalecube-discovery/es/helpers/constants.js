import { getFullAddress } from '@scalecube/utils';
export var INVALID_ITEMS_TO_PUBLISH = 'itemsToPublish are not of type Array';
export var NODEJS_MUST_PROVIDE_CLUSTER_IMPL = 'Must provide cluster when running on nodejs';
export var getAddressCollision = function (address, seedAddress) {
    return "address " + address + " must be different from the seed Address " + seedAddress;
};
export var getDiscoverySuccessfullyDestroyedMessage = function (address) {
    return getFullAddress(address) + " has been removed";
};
